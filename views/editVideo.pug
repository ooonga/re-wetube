extends layouts/main

block content
  .form-container
    form(action="/videos"+routes.editVideo, method="post")
      input(type="text", name="title", placeholder="Title")
      textarea(name="disciption", placeholder="Discription")
      input(type="submit", value="Update Video")
    a.form-container_link.form-container_link-delete(href="/video"+routes.deleteVideo) Dlete Video